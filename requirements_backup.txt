# Core DL (RTX 5080 + PyTorch 2.7.0 + CUDA 12.8) - RTX 5080 sm_120 지원

# PyTorch stack (정확한 페어링 필수)
# 설치: pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128
torch==2.7.0+cu128
torchvision==0.22.0+cu128
# torchaudio==2.7.0+cu128  # 필요시 주석 해제

# Vision & ML frameworks
timm>=1.0.11           # PyTorch 2.7.0 호환 (공식 지원)
ultralytics>=8.3.0     # PyTorch 2.7.0 호환, YOLO11 지원

# Computer Vision
numpy>=2.1.0,<2.2      # PyTorch 2.7.0 호환 (numpy 2.x 지원)
pillow>=10.0,<12.0
opencv-python-headless>=4.9,<5.0
albumentations>=1.4,<2.0  # 프레임워크 비종속
kornia>=0.7,<1.0       # PyTorch 의존성만 있음

# Data & Config
pyyaml>=6.0,<7.0
tqdm>=4.66,<5.0
pandas>=2.0,<3.0
scikit-learn>=1.4,<2.0

# Logging & Monitoring
tensorboard>=2.15,<3.0
wandb>=0.16,<1.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0

# API & Service
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0

# Export & ONNX (GPU 버전으로 통일)
onnx>=1.17.0
onnxruntime-gpu>=1.20.0  # CUDA 12.8 호환, TensorRT EP 지원

# Memory & Performance (128GB RAM 활용)
lmdb>=1.4,<2.0
psutil>=6.0,<8.0
gputil>=1.4,<2.0      # GPU 모니터링 (가벼운 psutil 호환 라이브러리)

# Version compatibility & fallback support
pkg_resources          # 버전 체크용
subprocess-run>=0.1.0  # Git SHA 획득용

# Testing
httpx>=0.26,<1.0
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0

# TensorRT (선택적 설치)
# tensorrt>=10.9         # ORT 1.22 호환성 매트릭스 기준